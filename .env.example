# Copy to .env locally. Never commit real values.

# Plan A (Cursor environment / local)
APIFY_TOKEN=
HARVEST_API_KEY=
NOCO_TOKEN=
NOCO_BASE=https://mpa.parvusmedia.com

# Plan B (n8n Variables)
N8N_URL=https://pmedia.app.n8n.cloud
# REST API key, JWT aud=public-api — not the MCP token
N8N_REST_API_KEY=
# MCP token (aud=mcp-server-api); cannot read Variables
N8N_API_KEY=
