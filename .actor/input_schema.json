{
  "title": "Full Company Identity Resolver Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "queries": {
      "title": "Queries",
      "description": "Newline-separated Spanish company legal names. Used when companies is empty. Compatible with the previous Actor.",
      "type": "string",
      "editor": "textarea",
      "prefill": "Albrok Mediacion S.A.\nOtra Empresa S.L."
    },
    "companies": {
      "title": "Companies",
      "description": "Structured company list. When provided, takes priority over queries.",
      "type": "array",
      "editor": "json",
      "items": {
        "type": "object",
        "properties": {
          "source_id": {
            "title": "Source ID",
            "description": "External identifier from the source CRM or system.",
            "type": "string",
            "editor": "textfield"
          },
          "legal_name": {
            "title": "Legal name",
            "description": "Company legal name / razón social.",
            "type": "string",
            "editor": "textfield"
          },
          "tax_id": {
            "title": "Tax ID",
            "description": "Spanish tax identifier (NIF/CIF), optional.",
            "type": "string",
            "editor": "textfield"
          },
          "city": {
            "title": "City",
            "description": "City hint used for scoring and disambiguation.",
            "type": "string",
            "editor": "textfield"
          },
          "province": {
            "title": "Province",
            "description": "Province hint used for scoring and disambiguation.",
            "type": "string",
            "editor": "textfield"
          },
          "country": {
            "title": "Country",
            "description": "Country hint. Defaults to España when omitted.",
            "type": "string",
            "editor": "textfield",
            "default": "España"
          }
        },
        "required": ["legal_name"]
      }
    },
    "apify_token": {
      "title": "Apify token",
      "description": "Optional fallback Apify API token. Prefer APIFY_TOKEN environment variable. Never logged.",
      "type": "string",
      "editor": "textfield",
      "isSecret": true
    },
    "google_actor_id": {
      "title": "Google Actor ID",
      "description": "Apify Actor used for Google Search. Default: apify/google-search-scraper.",
      "type": "string",
      "editor": "textfield",
      "default": "apify/google-search-scraper"
    },
    "google_results_per_page": {
      "title": "Google results per page",
      "description": "Number of organic results requested per Google query.",
      "type": "integer",
      "editor": "number",
      "default": 10,
      "minimum": 1,
      "maximum": 100
    },
    "country_code": {
      "title": "Country code",
      "description": "Google Search countryCode, e.g. es.",
      "type": "string",
      "editor": "textfield",
      "default": "es"
    },
    "language_code": {
      "title": "Language code",
      "description": "Google Search languageCode, e.g. es.",
      "type": "string",
      "editor": "textfield",
      "default": "es"
    },
    "max_harvest_candidates": {
      "title": "Max Harvest candidates",
      "description": "Maximum LinkedIn candidates enriched with HarvestAPI per company (1-3 recommended).",
      "type": "integer",
      "editor": "number",
      "default": 2,
      "minimum": 1,
      "maximum": 5
    },
    "harvest_api_key": {
      "title": "Harvest API key",
      "description": "Optional fallback HarvestAPI key. Prefer HARVEST_API_KEY environment variable. Never logged.",
      "type": "string",
      "editor": "textfield",
      "isSecret": true
    },
    "harvest_concurrency": {
      "title": "Harvest concurrency",
      "description": "Maximum concurrent HarvestAPI requests.",
      "type": "integer",
      "editor": "number",
      "default": 3,
      "minimum": 1,
      "maximum": 10
    },
    "fallback_google_by_website": {
      "title": "Fallback Google by website",
      "description": "If confidence is low, run a third Google query: site:linkedin.com/company \"domain.com\".",
      "type": "boolean",
      "editor": "checkbox",
      "default": true
    },
    "fallback_confidence_threshold": {
      "title": "Fallback confidence threshold",
      "description": "Run domain fallback Google search when selected confidence is below this value.",
      "type": "integer",
      "editor": "number",
      "default": 78,
      "minimum": 0,
      "maximum": 100
    },
    "use_ai_for_ambiguous": {
      "title": "Use AI for ambiguous cases",
      "description": "Optionally ask OpenAI to choose among ambiguous candidates. Deterministic scoring remains primary.",
      "type": "boolean",
      "editor": "checkbox",
      "default": false
    },
    "openai_api_key": {
      "title": "OpenAI API key",
      "description": "Optional fallback OpenAI key. Prefer OPENAI_API_KEY environment variable. Never logged.",
      "type": "string",
      "editor": "textfield",
      "isSecret": true
    },
    "openai_model": {
      "title": "OpenAI model",
      "description": "Model used when AI disambiguation is enabled.",
      "type": "string",
      "editor": "textfield",
      "default": "gpt-4o-mini"
    },
    "ai_confidence_threshold": {
      "title": "AI confidence threshold",
      "description": "Invoke AI only when deterministic confidence is below this value.",
      "type": "integer",
      "editor": "number",
      "default": 78,
      "minimum": 0,
      "maximum": 100
    },
    "batch_size": {
      "title": "Batch size",
      "description": "Maximum number of Google queries grouped into a single remote Actor run.",
      "type": "integer",
      "editor": "number",
      "default": 20,
      "minimum": 1,
      "maximum": 200
    },
    "debug": {
      "title": "Debug",
      "description": "When true, include candidates, website_candidates, ai_decision and raw_harvest in the output row.",
      "type": "boolean",
      "editor": "checkbox",
      "default": false
    }
  }
}
